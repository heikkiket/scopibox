extend type Query {
  randomVideo: Video
}

extend type Mutation {
  addVideo(title: String, url: String): Video
}

type Video {
  url: String
  embedUrl: String
  title: String
  description: String
}

