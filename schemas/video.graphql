extend type Query {
  randomVideo: Video
  videoHistory(): [Video]
}

extend type Mutation {
  addVideo(title: String, url: String): Video
}

type Video {
  url: String
  title: String
  description: String
}
